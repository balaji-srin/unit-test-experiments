:cmock:
    :plugins:
        - :ignore_arg
        - :return_thru_ptr
    :treat_inlines:
        :include
    :strippables:
        # Disable mocking of variadic function variadic_module_get
        - ['(?:variadic_module_get\s*\(+.*?\)+)']
